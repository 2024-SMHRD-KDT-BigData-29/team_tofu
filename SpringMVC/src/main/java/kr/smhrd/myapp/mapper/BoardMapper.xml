<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.smhrd.myapp.mapper.BoardMapper">
	
	<select id="boardList" resultType="kr.smhrd.myapp.entity.Board">
		select * from board
	</select>
	
	<delete id="deleteBoard" parameterType="int">
		delete from board where idx=#{idx}
	</delete>
	
	
	<insert id="insertBoard" parameterType="kr.smhrd.myapp.entity.Board">
		insert into board(title, writer, filename, content)
		values(#{title}, #{writer}, #{filename}, #{content})
	</insert>
	
	<select id="boardDetail" parameterType="int" resultType="kr.smhrd.myapp.entity.Board">
		select * from board where idx=#{idx}
	</select>
</mapper>